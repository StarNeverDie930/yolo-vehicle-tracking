## 1. 统一车辆 taxonomy

- [x] 1.1 新增或整理单一的车辆 taxonomy 来源，供检测、绘制、统计、配置和文档共享
- [x] 1.2 更新 `core/detector.py`、`utils/drawing.py`、`analysis/counter.py`、`app/result_panel.py`，移除各自独立的类别表
- [x] 1.3 统一 `scripts/convert_detrac_to_yolo.py`、`scripts/merge_datasets.py`、`scripts/detrac.yaml`、`scripts/merged.yaml` 的类别顺序和命名
- [x] 1.4 同步 `config.yaml` 与 `README.md` 中的类名与语义说明；`docs/任务书.md`、`docs/开题报告.md` 作为基线文档仅复核不改动

## 2. 调整计数与热力图语义

- [x] 2.1 恢复或实现 `unique_track` 与 `line_crossing` 两种计数模式
- [x] 2.2 让 `line_position`、`direction` 和流量窗口真正参与计算
- [x] 2.3 将流量窗口从墙钟时间改为视频时间，并保留必要的兜底值
- [x] 2.4 拆分轨迹显示缓存与热力图累积数据，热力图以完整运行过程为输入
- [x] 2.5 支持保存原始热力图和 overlay 热力图到结果目录

## 3. 扩展分析结果输出

- [x] 3.1 输出车型结构统计，包括累计数量和占比
- [x] 3.2 输出分时段流量趋势，按视频时间分桶统计总量和分车型数量
- [x] 3.3 输出轨迹摘要，包括 track_id、类别、出现帧范围、持续时长、轨迹点数和像素路径长度
- [x] 3.4 输出热点区域排名，将热力图按网格统计并列出高密度区域
- [x] 3.5 输出处理性能摘要，包括视频帧数、处理耗时、平均 FPS、模型路径和设备信息

## 4. 扩展评估与报告

- [x] 4.1 为评估结果保留更完整的元信息：模型路径、序列名、模式、时间戳、环境与性能数据
- [x] 4.2 扩展评估覆盖，至少补充公开序列与自采视频的验证记录
- [x] 4.3 保留图表生成，但确保图表失败不阻断摘要写入
- [x] 4.4 补充或整理适合论文展示的汇总输出，便于直接引用到报告中

## 5. 同步文档与仓库规则

- [x] 5.1 更新 `README.md`，消除 PyQt6 / PySide6、计数定义和评估范围的表述冲突；任务书和开题报告保持原样
- [x] 5.2 调整 `.gitignore`，让 Markdown 版毕业设计文档可进入版本控制
- [x] 5.3 检查基线文档中的截图、术语和功能描述，记录与实现差异但不修改任务书和开题报告正文

## 6. 提升处理稳定性

- [x] 6.1 为视频打开、读帧、写入和导出补充显式异常处理
- [x] 6.2 在处理失败时恢复 GUI 按钮状态，并向用户显示明确错误信息
- [x] 6.3 保留模型切换与处理互斥的约束，并把失败路径纳入手工验证清单

## 7. 验证

- [x] 7.1 用一个公开序列验证检测、跟踪、计数、热力图、分析结果和导出结果是否与新 taxonomy 一致
- [x] 7.2 用一个自采视频验证实时性、统计结果、分析结果和错误反馈
- [x] 7.3 检查摘要输出、图表输出、热力图保存结果和文档是否能直接用于毕业设计材料
