## 1. 重构 worker 生命周期

- [x] 1.1 将 `ProcessingWorker.finished` 自定义业务信号改名，避免与 `QThread.finished` 冲突
- [x] 1.2 为正常完成、用户取消和异常失败定义清晰的 worker 信号
- [x] 1.3 确保 `self.worker` 只在线程真实结束后清理，不在业务完成回调中提前置空
- [x] 1.4 为 worker 添加安全清理路径，保证 `VideoReader`、`VideoWriter` 和 Qt 线程都能正常释放

## 2. 建立 GUI 状态机

- [x] 2.1 引入处理状态字段，覆盖未打开视频、已打开、处理中、停止中、处理完成未导出、处理完成已导出、导出中和错误恢复
- [x] 2.2 抽取统一的控件状态刷新方法，集中管理打开、开始、停止、导出、查看热力图、保存热力图按钮
- [x] 2.3 修正 `hasattr(self, "video_path")` 这类空路径判断，统一使用 `self.video_path` 的真实值
- [x] 2.4 处理完成后停止按钮必须置灰，并允许导出视频、打开新视频、查看热力图和保存热力图

## 3. 完成未保存视频提醒

- [x] 3.1 普通处理完成后标记 `processing_completed=True`、`processed_video_saved=False`
- [x] 3.2 导出视频完成后记录导出路径，并标记 `processed_video_saved=True`
- [x] 3.3 打开新视频前，如果已有处理完成但未导出的视频，弹窗提示用户确认是否继续
- [x] 3.4 用户取消继续时不打开文件选择窗口，用户确认后再进入视频选择流程

## 4. 修正处理、导出、停止和关闭流程

- [x] 4.1 正常处理完成后弹窗提示完成，并显示分析结果目录
- [x] 4.2 导出视频期间进入 busy 状态，禁止重复处理、重复导出、打开新视频和热力图操作
- [x] 4.3 点击停止后进入停止中状态，并在 worker 退出后提示取消或恢复界面，不误报正常完成
- [x] 4.4 处理或导出失败时恢复到可操作状态，并显示明确错误信息
- [x] 4.5 实现窗口关闭保护：后台线程运行时提示用户，确认后安全停止并等待线程退出

## 5. 拆分热力图查看和保存

- [x] 5.1 将热力图交互拆成“查看热力图”和“保存热力图”两个动作
- [x] 5.2 查看热力图只更新画面，不写文件
- [x] 5.3 保存热力图将原始热力图和 overlay 热力图写入当前结果目录
- [x] 5.4 在无处理结果或无热力图数据时禁用相关动作或给出明确提示

## 6. 验证

- [x] 6.1 运行 `python main.py`，处理一个测试视频直到 100%，确认无 `QThread: Destroyed while thread is still running`
- [x] 6.2 验证处理完成弹窗、停止按钮置灰、导出视频、打开新视频、查看热力图和保存热力图均可用
- [x] 6.3 验证处理完成但未导出视频时打开新视频会出现确认弹窗
- [x] 6.4 验证导出视频期间不能重复点击导出、打开新视频或切换任务
- [x] 6.5 验证停止处理不会误报处理完成
- [x] 6.6 验证后台线程运行时关闭窗口会提示并安全退出
